interface Config {
  tabWidth: number
  printWidth: number
  semi: boolean
  singleQuote: boolean
}

const config: Config = {
  tabWidth: 2,
  printWidth: 80,
  semi: false,
  singleQuote: true,
}

export default config
